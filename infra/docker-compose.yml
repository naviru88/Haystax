services:
  rabbitmq:
    image: rabbitmq:3.13-management
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: haystax
      RABBITMQ_DEFAULT_PASS: haystax

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
